export type Cotizacion = {
  _id: string;
  file: string;
  fileName: string;
  firstName: string;
  lastName: string;
  email: string;
  body: string;
  phoneNumber: string;
  active: boolean;
  createdAt: string;
};
